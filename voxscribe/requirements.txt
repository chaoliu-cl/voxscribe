# Runtime dependencies for Python 3.11+
faster-whisper>=1.2.1
numpy>=2.4.3
soundfile>=0.13.1
PySide6>=6.11.0
torch>=2.11.0
pandas>=3.0.1
matplotlib>=3.10.8,<4.0.0
networkx>=3.6.1

# Optional audio conversion support
# Install manually only if you need format conversion via ffmpeg/pydub.
# pydub>=0.25.1

# GPU users should install the torch wheel that matches their CUDA runtime.
# See: https://pytorch.org/get-started/locally/
